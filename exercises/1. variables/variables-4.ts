// variables-4.ts
//
// Make me work!
//
// exercise() should set variables 'estimatedSalary' and 'numberOfJobOffers' inside the if block
// and return both variables outside the if block
//
// Use the `hint` watch subcommand for a hint
//
// @ts-nocheck
//
// I AM NOT DONE

export function exercise() {
  const isGoodAtCoding: boolean = true;

  if (isGoodAtCoding) {
    var estimatedSalary: number = 1_000_000;
    let numberOfJobOffers: number = 100;
  }

  // Do not change this line
  return { estimatedSalary, numberOfJobOffers };
}
